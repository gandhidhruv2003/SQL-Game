create database if not exists game;
use game;

create table person (
	person_id varchar(30) primary key,
    name varchar(255),
    email varchar(255),
    phone_number int
);

create table demography(
	
)
